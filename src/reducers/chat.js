const initialState = {
    chatId: null,
    participants: [],
    message: [],
}

export default function chatReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}